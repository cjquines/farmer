{
  "$schema": "https://json.schemastore.org/package",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "parse-types": "tsx scripts/parse-types.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync": "tsx scripts/sync.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
